# Context

Don't commit changes to git. The user will do that as needed.

This project involves working inside the OpenShell sandbox managed by the
scripts in the working directory.

The source to OpenShell is at the root directory and contains skills for solving
problems with OpenShell.

openshell.md is being used to document the process the user must follow to use
the sandbox in vscode. Keep it up to date and terse. Do not document past
misunderstandings or why new knowledge was added, keep it terse.
